/**
 * @tg-feed/web — entrypoint placeholder.
 *
 * Chapter 9 (see docs/PLAN.md) replaces this with a Vite + React + Tailwind
 * + shadcn/ui setup (`main.tsx`, `App.tsx`, pages, components, etc.).
 *
 * Until then this exists only so the workspace typechecks.
 */
import { SHARED_PACKAGE_VERSION } from '@tg-feed/shared';

export const WEB_PLACEHOLDER = `web@${SHARED_PACKAGE_VERSION}`;
